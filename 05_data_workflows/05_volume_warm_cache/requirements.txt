# VolumeCache ships in runpod-python via SLS-367 / PR #538 (merged), released in
# runpod 1.12.0. Earlier versions do not have it.
runpod>=1.12.0

runpod-flash>=1.8.0

# Model libraries used by the GPU worker
torch
diffusers
transformers
accelerate
