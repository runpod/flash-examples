[project]
name = "02_langgraph_basics"
version = "0.1.0"
description = "LangGraph + Flash integration example for distributed AI workflows"
requires-python = ">=3.10"
dependencies = [
    "tetra_rp",
    "langgraph>=0.2.0",
    "langchain-core>=0.3.0",
    "numpy>=2.0.0",
]
