[project]
name = "runpod-public-api-example"
version = "0.1.0"
description = "Generate images using Runpod public text-to-image endpoint"
requires-python = ">=3.10"
dependencies = [
    "tetra_rp",
]
